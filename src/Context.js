import { createContext } from "react";

/**
 * Context.js
 * * IMPORTANT: We use a named export 'userContext' here.
 * This must match the import in App.js: import { userContext } from "./Context";
 */
export const userContext = createContext();
